[project]
name = "hegselmann2005-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "hegselmann-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
hegselmann-tools = { workspace = true }
